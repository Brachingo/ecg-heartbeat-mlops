services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models   # monta el checkpoint entrenado sin reconstruir la imagen
    environment:
      - PYTHONPATH=/app/src
    restart: unless-stopped
